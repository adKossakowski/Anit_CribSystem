package Interfaces;

public interface Operation_Interface <T> {

	T addition(T left, T right);
	
	T substraction(T left, T right);
	
	T multiplication(T left, T right);
	
	T division(T left, T right);
	
	T attribution();
}
